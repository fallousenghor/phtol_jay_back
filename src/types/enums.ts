export enum Role {
  USER = 'USER',
  MODERATEUR = 'MODERATEUR',
  ADMIN = 'ADMIN'
}

export enum Action {
  APPROVED = 'APPROVED',
  REJECTED = 'REJECTED'
}

export enum NotificationType {
  REPUBLISH = 'REPUBLISH',
  GENERAL = 'GENERAL'
}
